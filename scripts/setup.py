from setuptools import setup

setup(name='pyetl',
      version='0.0.1',
      description='PySpark ETL Application for test automation',
      author='suriyakrishna',
      packages=['utils'],
      zip_safe=False)